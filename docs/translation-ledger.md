# Translation Ledger

Status values: `copied`, `translated`, `stubbed`, `optimized`, `replaced`, `intentionally unsupported`.

| Upstream path/function | Local path/function | Status | Parity proof | Known deviations and source-cited reason |
| --- | --- | --- | --- | --- |
| `libavcodec/h264.h` NAL constants | `internal/h264/nal.go` `NALUnitType` constants | translated | unit | H.264-only constants; shared FFmpeg codec constants outside decoder scope are omitted. |
| `libavcodec/h2645_parse.c` `ff_h2645_extract_rbsp` | `internal/h264/nal.go` `AppendRBSP` | translated | unit | Keeps H.264 RBSP unescaping behavior; HEVC/VVC naming, FFmpeg padding, and skipped-byte position bookkeeping are deferred until a decoder slice needs them. |
| `libavcodec/get_bits.h` `GetBitContext` read/skip helpers | `internal/h264/bitreader.go` `bitReader` | translated | unit | MSB-first bounded reader only; FFmpeg cached-reader macros and unaligned fast paths are intentionally not optimized yet. |
| `libavcodec/golomb.h` `get_ue_golomb*`, `get_se_golomb_long` | `internal/h264/golomb.go` | translated | unit | Table-accelerated VLC paths are not ported yet; semantics are preserved with direct bit reads before optimization. |
| `libavcodec/mpegutils.h`, `h264_parse.h` MB type flags | `internal/h264/data.go` MB type constants | translated | unit | H.264-relevant flags only; non-H.264 aliases stay out of decoder scope. |
| `libavcodec/h264data.c` MB type and quant/dequant tables | `internal/h264/data.go` table constants and generators | translated | unit | Chroma-QP table is generated from FFmpeg's macro expansion shape with spot checks across bit depths. |
| `libavcodec/h264_ps.c` `ff_h264_decode_seq_parameter_set` through cropping | `internal/h264/sps.go` `DecodeSPS` | translated | unit, `ffprobe` oracle | SPS parsing currently proves metadata through dimensions/crop and records the VUI presence bit. Full VUI/HRD is deferred; scaling matrices are now stored source-shaped. |
| `libavcodec/h264_ps.c` `ff_h264_decode_picture_parameter_set` syntax fields plus QP/dequant setup | `internal/h264/pps.go` `DecodePPS`, `buildQPTable`, `initDequantTables` | translated | unit, `ffprobe` fixture exercised through header parse | PPS syntax fields through chroma QP offsets are parsed against active SPS state. FMO remains intentionally unsupported, matching FFmpeg's missing-feature failure path here. |
| `libavcodec/h264_slice.c` `h264_slice_header_parse` through macroblock payload handoff | `internal/h264/slice.go` `ParseSliceHeader` | translated | unit, `ffprobe` fixture exercised through header parse | Stops before macroblock decode. IDR `idr_pic_id >= 65536` follows FFmpeg's warning-only behavior by leaving the stored value unset. |
| `libavcodec/h264_parse.c` `ff_h264_parse_ref_count` | `internal/h264/slice.go` `parseRefCount` | translated | unit | Picture-structure max refs and I/P/B list-count behavior are source-shaped. |
| `libavcodec/h264_refs.c` `ff_h264_decode_ref_pic_list_reordering`, `ff_h264_decode_ref_pic_marking` syntax fields | `internal/h264/slice.go` `decodeRefPicListReordering`, `decodeRefPicMarking` | translated | unit, fixture path for IDR marking | Stores parsed syntax only; applying reference-list mutations waits for DPB/reference picture port. |
| `libavcodec/h264_parse.c` `ff_h264_pred_weight_table` syntax fields | `internal/h264/slice.go` `predWeightTable` | translated | not yet fixture-covered | Stores explicit weights and MBAFF copies; implicit weighted bipred waits for reference-list/DPB port. |
| `libavcodec/h264_cavlc.c` static VLC tables and `init_cavlc_level_tab` | `internal/h264/cavlc.go` CAVLC tables, VLC reader, level table | translated | unit | Dense FFmpeg `VLCElem` allocation layout is not copied yet; bounded MSB-first code semantics are source-shaped and optimization waits for macroblock parity. |
| `libavcodec/h264_cavlc.c` `decode_residual` coefficient syntax | `internal/h264/cavlc.go` `decodeCAVLCResidual` | translated | unit | Ported as an isolated residual primitive with caller-supplied prediction, scan, qmul, and block storage. Slice/macroblock state integration is pending. |
| `libavcodec/h264_parse.h` `scan8`, `h264data.c` chroma DC scans, `h264_slice.c` CAVLC scan tables | `internal/h264/data.go` scan tables | translated | unit | Frame/field scan table initialization is source-shaped for CAVLC; transform-bypass q0 selection waits for macroblock integration. |
| `libavcodec/h264_cavlc.c` `pred_non_zero_count`, `decode_luma_residual` | `internal/h264/cavlc_luma.go` | translated | unit | Luma residual syntax and `non_zero_count_cache` updates are isolated from full macroblock prediction/DPB state for now. |
| `libavcodec/h264_cavlc.c` chroma residual section in `ff_h264_decode_mb_cavlc` | `internal/h264/cavlc_chroma.go` | translated | unit | Chroma DC/AC residual syntax for 4:2:0 and 4:2:2 is isolated from full macroblock prediction/DPB state for now. |
| `libavcodec/h264data.c` full CBP maps, `h264_cavlc.c` MB type/CBP/QP intra macroblock front end | `internal/h264/data.go`, `internal/h264/cavlc_mb.go` | translated | unit | Intra macroblock residual orchestration is wired to the CAVLC residual context. Inter motion, IntraPCM, MBAFF field scans, and neighbor prediction application remain pending. |
| `libavcodec/h264_cavlc.c` P/B-slice inter reference and MVD syntax | `internal/h264/cavlc_inter.go` | translated | unit | Stores reference indices and motion-vector deltas without applying prediction or reference-list side effects. Direct B motion is explicitly unsupported until DPB/mvpred integration lands. |
| `libavcodec/cabac.c`, `cabac_functions.h` CABAC arithmetic decoder tables and primitives | `internal/h264/cabac_tables.go`, `internal/h264/cabac.go`, `scripts/generate-cabac-tables.py` | translated | unit, compiled C oracle | Tables are generated from the pinned FFmpeg source. The opt-in CABAC oracle compiles the pinned upstream arithmetic reader and compares aligned/unaligned primitive traces. Go bounds handling is conservative; CABAC macroblock integration remains pending. |
| `libavcodec/h264_cabac.c` `ff_h264_init_cabac_states` and init tables | `internal/h264/cabac_tables.go`, `internal/h264/cabac.go` `initH264CABACStates` | translated | unit | Context-init tables are generated from the pinned FFmpeg source. CABAC macroblock cache integration remains pending. |
| `libavcodec/h264_cabac.c` CABAC macroblock type, sub-type, CBP, ref-index, MVD, and dQP syntax helpers | `internal/h264/cabac_mb.go` | translated | unit | Helpers preserve FFmpeg context selection and branch order, but stop before full macroblock cache side effects, prediction, direct motion, residual orchestration, and reconstruction. |
| `libavcodec/h264_cabac.c` `get_cabac_cbf_ctx`, `decode_cabac_residual_internal`, DC/non-DC wrappers | `internal/h264/cabac_residual.go` | translated | unit | Ports CBF context selection, significance/last scans, coefficient level/sign decode, and non-zero cache updates. Transform/write-back and macroblock integration are pending. |
| `libavcodec/h264_cabac.c` `decode_cabac_luma_residual` and chroma residual tail | `internal/h264/cabac_luma.go`, `internal/h264/cabac_chroma.go` | translated | unit | Orchestrates CABAC DC/AC residual syntax over local MB buffers and non-zero cache. Full macroblock integration, transform/write-back, and chroma444 call sites remain pending. |
| `libavcodec/h264_cabac.c` remaining CABAC macroblock/residual integration | none | stubbed | not yet proved | Pending after CABAC macroblock cache side effects and slice-level entropy handoff are ported. |
| `libavcodec/h264_slice.c` remaining slice decode | none | stubbed | not yet proved | Pending after reference-list application, prediction setup, and entropy slices. |
| `libavcodec/h264_mb.c`, `h264_direct.c`, `h264_refs.c`, `h264_picture.c` | none | stubbed | not yet proved | Pending after slice header and entropy parity. |
| `libavcodec/h264dsp*`, `h264idct*`, `h264pred*`, `h264qpel*`, `h264chroma*` | none | stubbed | not yet proved | Reference Go kernels first; SIMD/architecture dispatch only after byte parity. |
| `libavcodec/h264dec.c` public decoder integration | none | stubbed | not yet proved | Final raw-port integration layer after core state machines are translated. |
