# Translation Ledger

Status values: `copied`, `translated`, `stubbed`, `optimized`, `replaced`, `intentionally unsupported`.

| Upstream path/function | Local path/function | Status | Parity proof | Known deviations and source-cited reason |
| --- | --- | --- | --- | --- |
| `libavcodec/h264.h` NAL constants | `internal/h264/nal.go` `NALUnitType` constants | translated | unit | H.264-only constants; shared FFmpeg codec constants outside decoder scope are omitted. |
| `libavcodec/h2645_parse.c` `ff_h2645_extract_rbsp` | `internal/h264/nal.go` `AppendRBSP` | translated | unit | Keeps H.264 RBSP unescaping behavior; HEVC/VVC naming, FFmpeg padding, and skipped-byte position bookkeeping are deferred until a decoder slice needs them. |
| `libavcodec/h2645_parse.h` `get_nalsize`, `libavcodec/h2645_parse.c` `ff_h2645_packet_split` NALFF branch, `libavcodec/h264_parse.c` `ff_h264_decode_extradata` avcC/Annex B branches, `libavcodec/h264dec.c` `is_avc`/`nal_length_size` handoff and `AV_PKT_DATA_NEW_EXTRADATA` handling | `internal/h264/nal.go` `SplitAVCC`, `SplitAutoPacket`, `IsAVCDecoderConfigurationRecord`, `DecodeAVCDecoderConfigurationRecord`; `internal/h264/simple_decode.go` `SimpleDecoder`, `StoreParamSets`, `UpdateParamSets`, `DecodeAVCSimpleFrames`, `DecodeAVCSimpleFramesWithConfigurationRecord`, `FlushDelayedFrames`; `decoder.go` `Packet`, `PacketSideData`, `DecodePacket`, `DecodePacketFrames`, `Decode`, `DecodeFrames`, `DecodeAVC`, `DecodeAVCFrames`, `DecodeConfiguredAVC`, `DecodeConfiguredAVCFrames`, `FlushDelayedFrames`, `ParseHeadersAVC`, `ParseAVCDecoderConfigurationRecord`, `DecodeAVCWithConfigurationRecord`, `DecodeAVCFramesWithConfigurationRecord` | translated | unit, rawvideo MD5 fixture parity | Ports big-endian 1-4 byte length-prefixed NAL extraction plus MP4-style `avcC` and Annex B SPS/PPS extradata parsing and feeds both paths into the same NAL-unit decode loop. Configured AVC decoding now preserves simple DPB/reference state across successive packet calls for MP4-style samples, including one-sample-at-a-time B-frame streams that require an explicit delayed-frame flush. Generic public packet intake accepts Annex B and AVC4 packets, packet-level `avcC` configuration records, empty packets for delayed-frame flush, and FFmpeg's configured `nal_length_size == 4` AVC/Annex B sniffing branch before feeding the same NAL-unit decode loop. Public packet side-data intake applies `AV_PKT_DATA_NEW_EXTRADATA`-shaped `avcC` or Annex B parameter-set updates before non-empty packet splitting without resetting the simple DPB, matching FFmpeg's decode-frame ordering for this side-data type and preserving P-frame references when side data repeats across samples. The public avcC packet probe accepts valid standalone records with non-zero profile-compatibility flags that FFmpeg's frame-path wrapper does not probe until narrower gates pass; the record walk and parser semantics remain FFmpeg-shaped. The avcC fallback for invalid unescaped RBSP parameter sets is preserved by retrying parameter-set decode with the raw payload. The legacy explicit packet API still takes an explicit `nalLengthSize`; remaining packet side-data types beyond new extradata, A53/AFD, and S12M remain pending. |
| `libavcodec/decode.c` `side_data_map`, `ff_decode_frame_props_from_pkt` packet-to-frame mappings for `AV_PKT_DATA_A53_CC`, `AV_PKT_DATA_AFD`, and `AV_PKT_DATA_S12M_TIMECODE`; `libavcodec/packet.h` packet side-data enum values; H.264 frame-allocation side-data timing through `ff_get_buffer` and `h264_export_frame_props` | `decoder.go` `PacketSideDataA53ClosedCaptions`, `PacketSideDataActiveFormat`, `PacketSideDataS12MTimecode`, `decodeFrames`, `packetFrameSideDataFromPacket`, `packetSideDataGet`, `s12mTimecodesFromPacketSideData`, `frameSideDataFromH264`; `internal/h264/simple_decode.go` `SimpleDecoder.DecodeNALUnitsWithSideData`, `DecodedFrameSideData` | translated | unit, rawvideo MD5 fixture parity | Mirrors FFmpeg's packet side-data lookup order by using the first matching packet entry for each mapped type, attaches packet side data when the decoded frame is created so it follows delayed B-frame output, and preserves the H.264 export ordering where packet A53/AFD remain first while picture-timing S12M replaces packet S12M when coded timecode is present. A53 bytes are copied onto public frame side data, AFD consumes the one-byte active-format description, and S12M consumes the FFmpeg-documented four-`uint32_t` packet layout into public timecode words. Dynamic HDR10+, skip-samples, LCEVC, coded-side-data, metadata dictionaries, discard flags, timestamps, side-data preference options, and non-video/global side-data mappings remain outside the current H.264 decoder surface. |
| `libavcodec/get_bits.h` `GetBitContext` read/skip/align helpers | `internal/h264/bitreader.go` `bitReader` | translated | unit | MSB-first bounded reader with byte alignment for CAVLC IntraPCM payloads and CABAC slice startup's `(get_bits_left()+7)/8` remaining-byte handoff. FFmpeg cached-reader macros and unaligned fast paths are intentionally not optimized yet. |
| `libavcodec/golomb.h` `get_ue_golomb*`, `get_se_golomb_long` | `internal/h264/golomb.go` | translated | unit | Table-accelerated VLC paths are not ported yet; semantics are preserved with direct bit reads before optimization. |
| `libavcodec/mpegutils.h`, `h264_parse.h` MB type flags | `internal/h264/data.go` MB type constants | translated | unit | H.264-relevant flags only; non-H.264 aliases stay out of decoder scope. |
| `libavcodec/h264data.c` MB type, IntraPCM size, and quant/dequant tables | `internal/h264/data.go` table constants and generators | translated | unit | Chroma-QP table is generated from FFmpeg's macro expansion shape with spot checks across bit depths. IntraPCM sample counts mirror `ff_h264_mb_sizes`. |
| `libavcodec/h264_ps.c` `ff_h264_decode_seq_parameter_set`, `decode_vui_parameters`, `decode_hrd_parameters`; `libavcodec/h2645_vui.c` `ff_h2645_decode_common_vui_params` | `internal/h264/sps.go` `DecodeSPS`, `decodeVUIParameters`, `decodeHRDParameters`, `decodeCommonVUIParameters`, `H2645VUI`; `decoder.go` `StreamInfo`, `Frame` VUI fields; `internal/h264/simple_decode.go` `DecodedFrame` VUI fields | translated | unit, `ffprobe` oracle | SPS parsing covers metadata through dimensions/crop, scaling matrices, common VUI SAR/overscan/video-signal/color/chroma-location fields, timing info, NAL/VCL HRD arrays and delay lengths, `pic_struct_present_flag`, bitstream restriction, `num_reorder_frames`, and FFmpeg's level-based reorder fallback when no restriction is signaled. Public `StreamInfo` and decoded `Frame` metadata now expose the translated FFmpeg-normalized SPS VUI SAR, video range/format, color primaries/transfer/matrix, chroma-location, and timing fields. Hardware data-copy side effects and non-simple frame timing/interlace behavior remain pending. |
| `libavcodec/h264_sei.c` `ff_h264_sei_decode`, `ff_h264_sei_process_picture_timing`, `decode_picture_timing`, `decode_recovery_point`, `decode_buffering_period`, `decode_green_metadata`; `libavcodec/h2645_sei.c` `decode_registered_user_data`, selected common SEI messages, and `ff_h2645_sei_to_frame` side-data handoff; `libavcodec/h264_slice.c` `h264_export_frame_props` S12M timecode export and recovery-frame setup; `libavcodec/h264dec.c` `output_frame` recovery key promotion; `libavcodec/atsc_a53.c` `ff_parse_a53_cc`; `libavutil/display.c` display matrix helpers; `libavutil/timecode.c` `av_timecode_get_smpte` | `internal/h264/sei.go` `H264SEIContext.Decode`, `H264SEIPictureTiming.Process`, `decodeRegisteredUserData`, `decodeRegisteredUserDataClosedCaption`, selected `decode*` helpers; `internal/h264/simple_decode.go` leading `NALSEI` handling, `DecodedFrameSideData`, `applySimpleFrameTimingProps`; `internal/h264/simple_dpb.go` `applySimpleRecoveryPoint`; `decoder.go` `FrameSideData`, `S12MTimecodes`, `s12mTimecodesFromPictureTiming`, `avTimecodeGetSMPTE`, `Stereo3D`, `DisplayOrientation`, `MasteringDisplay`, `Frame` timing flags | translated | unit, native C/ffprobe oracle, rawvideo MD5 fixture parity | Ports byte-framed SEI payload type/size accumulation, strict direct parser errors, FFmpeg's non-fatal missing-SPS master error, picture-timing payload copy and HRD/pic-struct timecode processing, buffering period initial CPB delays, registered ITU-T T.35 ATSC `DTG1` AFD and `GA94` A53 closed-caption parsing including FFmpeg's cc-count/marker-byte truncation check and multi-message caption append behavior, recovery point, green metadata, unregistered x264 user data, display orientation, frame packing, alternative transfer, ambient viewing environment with FFmpeg's invalid-value checks, H.274 film-grain characteristics including component model tables and H.264 repetition-period lifetime, mastering display, and content light state. The simple decoder parses leading SEI into decoder state, keeps SEI errors non-fatal like FFmpeg without `AV_EF_EXPLODE`, snapshots parsed SEI onto the decoded frame before delayed-output queuing, consumes one-shot unregistered/A53/AFD, picture-timing, recovery-point, and non-repeating H.264 film-grain side data after export, exposes public frame side data for the translated SEI subset, projects picture-timing timecodes into SMPTE 12M words using FFmpeg/libavutil packing and VUI-derived frame-rate semantics, projects recovery-point count zero into public key-frame metadata while keeping IDR key barriers separate for delayed output, projects frame packing into FFmpeg-shaped stereo3D metadata, projects display orientation through the libavutil display-matrix math, maps mastering-display primaries into FFmpeg RGB order with validity flags, and applies FFmpeg `h264_export_frame_props` picture-timing derivation for public `repeat_pict`, interlaced, and top-field-first frame metadata in the simple frame-picture path. Late-SEI sample reporting and field/MBAFF timing/interlace effects remain pending. |
| `libavcodec/h264_ps.c` `ff_h264_decode_picture_parameter_set` syntax fields plus QP/dequant setup | `internal/h264/pps.go` `DecodePPS`, `buildQPTable`, `initDequantTables` | translated | unit, `ffprobe` fixture exercised through header parse | PPS syntax fields through chroma QP offsets are parsed against active SPS state. FMO remains intentionally unsupported, matching FFmpeg's missing-feature failure path here. |
| `libavcodec/h264_slice.c` `h264_slice_header_parse` through macroblock payload handoff | `internal/h264/slice.go` `ParseSliceHeader`, `parseSliceHeaderWithPayload` | translated | unit, `ffprobe` fixture exercised through header parse | Keeps the live post-header bitreader at the first macroblock payload bit for slice-data decode. IDR `idr_pic_id >= 65536` follows FFmpeg's warning-only behavior by leaving the stored value unset. |
| `libavcodec/h264_parse.c` `ff_h264_parse_ref_count` | `internal/h264/slice.go` `parseRefCount` | translated | unit | Picture-structure max refs and I/P/B list-count behavior are source-shaped. |
| `libavcodec/h264_refs.c` `ff_h264_decode_ref_pic_list_reordering`, `ff_h264_decode_ref_pic_marking` syntax fields | `internal/h264/slice.go` `decodeRefPicListReordering`, `decodeRefPicMarking` | translated | unit, fixture path for IDR marking | Parses ref-list modification and memory-management syntax for the decoder state, including long-term list modification op `2` and MMCO long-term op families. Field/MBAFF structures and full error-resilience behavior remain pending. |
| `libavcodec/h264_refs.c` `h264_initialise_ref_list`, `ff_h264_build_ref_list`, `ff_h264_execute_ref_pic_marking` simple progressive frame subset; `libavcodec/h264_slice.c` `implicit_weight_table(field=-1)`, `h264_select_output_frame` `last_pocs` reorder inference | `internal/h264/simple_dpb.go` `simpleFrameDPB`, `initImplicitBWeightTable` | translated | unit, FFmpeg frame-MD5 oracle fixture | Ports progressive frame-picture P-list default ordering for short refs followed by long refs, missing-active-ref default substitution, list0 short-ref reordering ops 0/1, long-ref reordering op 2, sliding-window marking over mixed short/long state, MMCO short2unused/long2unused/short2long/set-max-long/reset/current-long handling, FFmpeg-shaped POC type 0/1/2 tracking, B-list default sorting around current POC, identical B-list swap, B list0/list1 reordering, progressive frame-picture implicit B-weight table construction, direct-motion ref-entry provenance for decoded frames, dynamic `has_b_frames` growth from B pictures and POC gaps, and delayed display-order output for the simple public path. Field/MBAFF pictures, gap frames, field/MBAFF implicit weights, and FFmpeg's logging/error-resilience side effects remain pending. |
| `libavcodec/h264_parse.c` `ff_h264_pred_weight_table` syntax fields | `internal/h264/slice.go` `predWeightTable` | translated | unit, FFmpeg frame-MD5 oracle fixture | Stores explicit weights, MBAFF copies, and implicit-weight table slots consumed by motion compensation. Covered for simple P-slice explicit luma/chroma weights and non-neutral simple B-slice implicit bipred weights in the public Annex B path; MBAFF field copies for implicit weights remain pending with field/MBAFF decode. |
| `libavcodec/h264_cavlc.c` static VLC tables and `init_cavlc_level_tab` | `internal/h264/cavlc.go` CAVLC tables, VLC reader, level table | translated | unit | Dense FFmpeg `VLCElem` allocation layout is not copied yet; bounded MSB-first code semantics are source-shaped and optimization waits for macroblock parity. |
| `libavcodec/h264_cavlc.c` `decode_residual` coefficient syntax | `internal/h264/cavlc.go` `decodeCAVLCResidual` | translated | unit | Ported as an isolated residual primitive with caller-supplied prediction, scan, qmul, and block storage. Slice/macroblock state integration is pending. |
| `libavcodec/h264_parse.h` `scan8`, `h264data.c` chroma DC scans, `h264_slice.c` CAVLC/CABAC scan table initialization | `internal/h264/data.go` scan tables and qscale scan selectors | translated | unit, FFmpeg frame-MD5 oracle fixture | Frame scan table initialization is source-shaped for CAVLC/CABAC, including FFmpeg's transform-bypass qscale-0 switch to untransposed 4x4 scans, raw CAVLC 8x8 scans, and direct CABAC 8x8 scans. Field scan table selection remains pending with field/MBAFF decode. |
| `libavcodec/h264_mvpred.h` `pred_intra_mode` and `libavcodec/h264_parse.c` `ff_h264_check_intra4x4_pred_mode`, `ff_h264_check_intra_pred_mode` | `internal/h264/intra_prediction.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Ports prediction-mode min/DC fallback and FFmpeg's unavailable-neighbor rewrite/error tables. Frame-MB CAVLC/CABAC call sites now update the current intra4x4 mode cache in FFmpeg block order, preserve raw mode write-back, and then apply current-cache rewrites used for reconstruction. Logging side effects are represented as `ErrInvalidData`; MBAFF-only chroma special modes are preserved but not wired into full MBAFF decode yet. |
| `libavcodec/h264_cavlc.c` `pred_non_zero_count`, `decode_luma_residual` | `internal/h264/cavlc_luma.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Luma residual syntax and `non_zero_count_cache` updates are wired through the simple frame-MB path, including the `chroma_format_idc == 3` luma-shaped chroma-plane residual branches covered by deblock-disabled 16x16 and deblock-enabled 32x32 High 4:4:4 CAVLC public frame parity. CAVLC qscale-0 transform-bypass scan selection is covered by High 4:4:4 Predictive qp=0 yuv420p lossless frame parity. |
| `libavcodec/h264_cavlc.c` chroma residual section in `ff_h264_decode_mb_cavlc` | `internal/h264/cavlc_chroma.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Chroma DC/AC residual syntax for 4:2:0 and 4:2:2 is wired through the simple frame path, including qscale-0 transform-bypass raw chroma DC/AC storage covered by public lossless CAVLC frame parity. `chroma_format_idc == 0` cache clearing is covered through public monochrome CAVLC frame parity. |
| `libavcodec/h264data.c` full CBP maps, `h264_cavlc.c` MB type/CBP/QP intra macroblock front end | `internal/h264/data.go`, `internal/h264/cavlc_mb.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Intra macroblock residual orchestration is wired to the CAVLC residual context. The frame-MB CAVLC path now uses source-shaped per-block intra4x4 prediction-cache updates, with an isolated caller-supplied-prediction helper retained for primitive tests. CAVLC IntraPCM is handled by the frame-MB handoff. Inter motion, MBAFF field scans, and full error-resilience side effects remain pending. |
| `libavcodec/h264_cavlc.c` P/B-slice inter reference and MVD syntax | `internal/h264/cavlc_inter.go` | translated | unit | Stores reference indices and motion-vector deltas without applying prediction or reference-list side effects. Top-level B_Direct syntax and B 8x8 direct sub-macroblocks skip ref/MVD reads for the direct predictor while preserving non-direct subpartition refs/MVDs in FFmpeg list/subblock order. |
| `libavcodec/cabac.c`, `cabac_functions.h` CABAC arithmetic decoder tables and primitives | `internal/h264/cabac_tables.go`, `internal/h264/cabac.go`, `scripts/generate-cabac-tables.py` | translated | unit, compiled C oracle | Tables are generated from the pinned FFmpeg source. The opt-in CABAC oracle compiles the pinned upstream arithmetic reader and compares aligned/unaligned primitive traces. Go bounds handling is conservative; CABAC macroblock integration remains pending. |
| `libavcodec/h264_cabac.c` `ff_h264_init_cabac_states` and init tables | `internal/h264/cabac_tables.go`, `internal/h264/cabac.go` `initH264CABACStates` | translated | unit | Context-init tables are generated from the pinned FFmpeg source and are now consumed by CABAC slice-data startup. Remaining CABAC bitstream overread/error-resilience policy stays with slice-loop parity. |
| `libavcodec/h264_cabac.c` CABAC macroblock type, sub-type, CBP, ref-index, MVD, and dQP syntax helpers | `internal/h264/cabac_mb.go` | translated | unit | Helpers preserve FFmpeg context selection and branch order, but stop before invoking the macroblock cache/write-back, prediction, direct motion, residual orchestration, and reconstruction call sites. |
| `libavcodec/h264_cabac.c` `get_cabac_cbf_ctx`, `decode_cabac_residual_internal`, DC/non-DC wrappers | `internal/h264/cabac_residual.go` | translated | unit | Ports CBF context selection, significance/last scans, coefficient level/sign decode, and non-zero cache updates. Transform/write-back and macroblock integration are pending. |
| `libavcodec/h264_cabac.c` `decode_cabac_luma_residual` and chroma residual tail | `internal/h264/cabac_luma.go`, `internal/h264/cabac_chroma.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Orchestrates CABAC DC/AC residual syntax over local MB buffers and non-zero cache, including the FFmpeg monochrome branch that clears chroma caches without reading chroma residual syntax. CABAC `cbp_table` write-back now follows FFmpeg by preserving lower CBP plus luma/chroma DC-coded bits rather than CAVLC-style luma nonzero flags, which is covered by multi-MB CABAC 32x32 parity. Chroma444 residual call sites are covered by deblock-disabled 16x16 and deblock-enabled 32x32 High 4:4:4 public frame parity; qscale-0 transform-bypass scans and raw residual storage are covered by High 4:4:4 Predictive qp=0 yuv420p lossless CABAC frame parity. |
| `libavcodec/h264dec.c` `ff_h264_alloc_tables` macroblock cache table allocation and `mb2b_xy`/`mb2br_xy` setup | `internal/h264/macroblock_cache.go` `newMacroblockTables` | translated | unit | Frame-MB layout uses FFmpeg's padded `mb_stride`, `b_stride`, rolling `mb2br_xy`, and decoder-side residual/motion tables. Slice threading, FMO, MBAFF field remapping, and picture-pool ownership remain pending. |
| `libavcodec/h264_mvpred.h` `fill_decode_caches` intra/residual/motion frame-MB cache subset; `libavcodec/h264dec.c` `ff_h264_slice_context_init` motion-cache sentinels | `internal/h264/macroblock_cache.go` `fillIntraPredModeCaches`, `fillResidualDecodeCaches`, `fillMotionDecodeCaches`, `initMotionDecodeCacheSentinels` | translated | unit, FFmpeg frame-MD5 oracle fixture | Ports frame-MB neighbor cache population for intra prediction modes, non-zero counts, CABAC CBP defaults, motion/ref/MVD neighbors, B direct-cache side data, and FFmpeg's internal right-edge `ref_cache` sentinels used by `fetch_diagonal_mv` for B 8x8 subpartition prediction. MBAFF/field-picture remapping and bitstream-level call-site wiring remain pending. |
| `libavcodec/h264_slice.c` frame-MB cursor setup/advance and `h264_mvpred.h` `fill_decode_neighbors` plus `fill_decode_caches` handoff | `internal/h264/slice_macroblock.go` | translated | unit | Maps `first_mb_in_slice` to FFmpeg padded `mb_xy`, advances frame macroblocks, applies non-FMO slice-boundary neighbor masking, and composes intra/residual/motion cache fillers. MBAFF/field pictures, FMO, and error recovery remain pending. |
| `libavcodec/h264_slice.c` `decode_slice` simple frame-MB entropy/reconstruction loop | `internal/h264/slice_decode.go` `decodeFrameSliceData`, `decodeCAVLCFrameSlice`, `decodeCABACFrameSlice` | translated | unit, FFmpeg frame-MD5 oracle fixture | Dispatches CAVLC directly from the live payload bitreader or realigns/initializes CABAC from the remaining payload bytes before the FFmpeg order of `ff_h264_decode_mb_{cavlc,cabac}` followed by `ff_h264_hl_decode_mb`. Covered for simple 8-bit frame pictures with caller-owned residual/motion/intra decode work, CAVLC bit exhaustion/skip-run termination, CABAC terminating-bit EOS, running slice QP carry across macroblocks, Main-profile CABAC IDR/P, High 4:2:0 32x32 CAVLC/CABAC 8x8-DCT IDR/P, High 4:2:2 CAVLC/CABAC, High monochrome CAVLC/CABAC, deblock-disabled 16x16 High 4:4:4 Predictive CAVLC/CABAC, deblock-enabled 32x32 High 4:4:4 Predictive CAVLC/CABAC, and qp=0 High 4:4:4 Predictive yuv420p lossless CAVLC/CABAC public frame-MD5 parity. Row-threaded deblocking, error resilience, threading, CABAC overread policy, high-bit-depth, MBAFF/field pictures, and full DPB/reference setup remain pending. |
| `libavcodec/h264_loopfilter.c` `alpha_table`, `beta_table`, `tc0_table`, `fill_filter_caches`, `check_mv`, `filter_mb_dir`, `ff_h264_filter_mb` frame-picture path | `internal/h264/loop_filter.go` `filterFrame`, `fillLoopFilterCachesFrame`, `filterFrameMacroblockDir`, edge wrappers | translated | unit, FFmpeg frame-MD5 oracle fixture | Ports the generic 8-bit frame-picture loop-filter strength calculation and luma/chroma edge calls for 4:2:0/4:2:2 simple frames, luma-only monochrome filtering with chroma edges skipped for `chroma_format_idc == 0`, and FFmpeg's 4:4:4 branch that applies luma edge filters to Cb/Cr with chroma QPs. The row includes slice-boundary mode `disable_deblocking_filter_idc == 2`, intra/inter bS rules, motion/ref checks, QP averaging, chroma-QP offsets, and CAVLC 8x8DCT nnz fixups. The simple integration applies the filter after all frame macroblocks are reconstructed, which preserves final frame parity for the current non-threaded frame-picture subset without FFmpeg's row-time border exchange. MBAFF/field filtering, high-bit-depth, postponed/threaded filtering, and SIMD loop-filter-strength dispatch remain pending. |
| `libavcodec/h264dec.c` `h264_decode_frame`, `decode_nal_units`, `ff_h264_queue_decode_slice`, simple `h264_decode_frame` output handoff; `libavcodec/h264_slice.c` `h264_export_frame_props` simple frame-property derivation | `internal/h264/simple_decode.go` `SimpleDecoder`, `DecodeAnnexBSimple`, `DecodeAnnexBSimpleFrames`, `DecodeAVCSimpleFrames`, `DecodeAVCSimpleFramesWithConfigurationRecord`, `FlushDelayedFrames`, `DecodedFrameSideData`, `applySimpleFrameTimingProps`; `decoder.go` `Packet`, `DecodePacket`, `DecodePacketFrames`, `Decode`, `DecodeFrames`, `DecodeAnnexB`, `DecodeAnnexBFrames`, `DecodeAVC`, `DecodeAVCFrames`, `DecodeConfiguredAVC`, `DecodeConfiguredAVCFrames`, `FlushDelayedFrames`, `DecodeAVCWithConfigurationRecord`, `DecodeAVCFramesWithConfigurationRecord`, `Frame.AppendRawYUV`, `FrameSideData`, `Frame` timing flags | translated | unit, FFmpeg frame-MD5 oracle fixture | Publicly decodes the already-translated simple 8-bit frame-picture subset from Annex B, explicit AVC/NALFF length-prefixed packets, generic auto-detected Annex B/AVC4 packets, packets with `AV_PKT_DATA_NEW_EXTRADATA`-shaped `avcC` or Annex B parameter-set side data, or AVC packets configured by separate/packet-level `avcC` SPS/PPS extradata through planar frame output and proves embedded black16 IDR, IDR/P-skip, deblock-enabled `testsrc2` IDR/P, default-active-ref=2 `testsrc2`, Main-profile CAVLC weighted-P fade, Main-profile CABAC IDR/P, High 4:2:0 32x32 CAVLC/CABAC 8x8-DCT IDR/P, High 4:2:2 CAVLC/CABAC 16x16 IDR/P, High monochrome CAVLC/CABAC 16x16 IDR/P, deblock-disabled High 4:4:4 Predictive CAVLC/CABAC 16x16 IDR/P, deblock-enabled High 4:4:4 Predictive CAVLC/CABAC 32x32 IDR/P, qp=0 High 4:4:4 Predictive yuv420p lossless CAVLC/CABAC IDR/P, 16x16/32x32 no-skip non-direct B-frame CAVLC/CABAC, 16x16 temporal/spatial-direct B-frame CAVLC/CABAC, 16x16 non-neutral implicit-weight B-frame CAVLC/CABAC `testsrc2`, and 64x64 CAVLC/CABAC B 8x8 plus SPS-flagged B_SUB_4x4 spatial/temporal direct-sub streams against FFmpeg rawvideo MD5s. The High 4:2:0 32x32 8x8-DCT, High 4:2:2, High 4:4:4 Predictive, monochrome, lossless, non-direct B, temporal/spatial-direct B, and CAVLC/CABAC B 8x8/B_SUB_4x4 direct-sub fixtures are covered across Annex B, explicit AVC, one-shot configured AVC, and generic public packet intake where applicable; the non-B and B-frame configured fixtures also cover sample-by-sample APIs, with explicit and empty-packet delayed-frame flush proving retained future-reference output. The current reference path owns a simple progressive frame DPB for P, explicit non-direct B packets, temporal/spatial-direct B packets, and CAVLC/CABAC B 8x8/B_SUB_4x4 direct-sub packets, including default short/long list build/reordering, limited MMCO application, explicit weighted P prediction, POC-backed B-list construction, implicit B-weight construction, direct-motion reference provenance, FFmpeg-shaped POC-gap reorder inference, delayed output flush, leading SEI parsing with non-exploding error handling, public frame side-data snapshots/projection for the translated SEI subset including AFD/A53 captions, S12M timecodes, recovery-point key flags, stereo3D, display matrix, mastering-display validity, ambient viewing environment, and H.274 film grain characteristics, one-shot SEI side-data consumption after frame export, SPS VUI metadata on decoded frames, and picture-timing-derived `repeat_pict`, interlaced, and top-field-first frame metadata. Fields/MBAFF, threading, field/MBAFF implicit weights, packet side-data types beyond new extradata, A53/AFD, and S12M, complete SEI timing/interlace behavior beyond the simple frame-picture flags, S12M export, and recovery-point key promotion, and full libavcodec delayed-output semantics beyond the simple progressive public path remain unsupported. |
| `libavcodec/h264_mvpred.h` `write_back_non_zero_count`, `write_back_intra_pred_mode`, `write_back_motion_list`, `write_back_motion` | `internal/h264/macroblock_cache.go` write-back helpers | translated | unit | Source-shaped copy order and table indices are covered for 4:2:0/4:2:2 residual rows, intra4x4 modes, MV/ref/MVD write-back, unused-list refs, and B direct-table entries. Reconstruction call sites are not wired yet. |
| `libavcodec/h264_mvpred.h` `fetch_diagonal_mv`, `pred_motion`, `pred_16x8_motion`, `pred_8x16_motion`, `pred_pskip_motion` frame-MB paths | `internal/h264/motion_prediction.go` | translated | unit | Ports frame-MB motion-vector prediction over the local motion cache and tables. MBAFF field/frame MV remapping remains pending. |
| `libavcodec/h264_direct.c` `ff_h264_direct_dist_scale_factor`, `pred_temp_direct_motion`, `pred_spatial_direct_motion`; `h264_mvpred.h` B-skip branch of `decode_mb_skip` | `internal/h264/direct_motion.go`, `internal/h264/macroblock_state.go`, frame-MB CAVLC/CABAC handoff | translated | unit, FFmpeg frame-MD5 oracle fixture | Ports progressive frame-picture temporal and spatial direct prediction for top-level B_Direct, B-skip, and CAVLC/CABAC B 8x8 direct sub-macroblocks, including decoded-frame macroblock table retention, colocated ref-list mapping back to current list0, FFmpeg scale-factor math, spatial neighbor-ref median prediction, colocated zero-motion collapse, direct_8x8_inference_flag=0 B_SUB_4x4 temporal per-block scaling and spatial per-block col-zero handling, cache fill, CAVLC direct ref/MVD elision, CABAC MVD zeroing, state write-back, and reconstruction/motion-comp consumption with direct flags preserved. MBAFF/field remapping and reference progress waits remain pending. |
| `libavcodec/h264_cavlc.c` inter motion-cache fill inside `ff_h264_decode_mb_cavlc` | `internal/h264/motion_prediction.go` `fillCAVLCInterMotionCache` | translated | unit, FFmpeg frame-MD5 oracle fixture | Applies decoded CAVLC refs/MVDs to 16x16, 16x8, 8x16, and 8x8 sub-MB cache rectangles with FFmpeg int16 write-back semantics, including direct-sub top-left ref repair and the slice-context diagonal sentinel needed by mixed B 8x8 subpartition prediction. Full complex slice call-site wiring remains pending. |
| `libavcodec/h264_cavlc.c` final MB table state writes and `h264_mvpred.h` `decode_mb_skip` P-skip/B-skip frame-MB write-back | `internal/h264/macroblock_state.go` | translated | unit, FFmpeg frame-MD5 oracle fixture | Writes CBP, qscale, MB type, slice table, intra4x4 modes, non-zero counts, CAVLC 8-bit IntraPCM state, CAVLC inter motion, P-skip motion, and progressive temporal/spatial B-skip direct motion into decoder-side tables. MBAFF and full complex slice-loop call-site wiring remain pending. |
| `libavcodec/h264_cavlc.c` `ff_h264_decode_mb_cavlc` frame-MB entropy-to-state order | `internal/h264/cavlc_frame.go` `decodeCAVLCFrameSliceMacroblock`, `decodeCAVLCFrameMacroblock` | translated | unit, FFmpeg frame-MD5 oracle fixture | Handles P-slice `mb_skip_run` and P-skip write-back, B-slice skip-run temporal/spatial direct write-back, FFmpeg's running `sl->qscale` across macroblocks, reads the CAVLC MB type for non-skip macroblocks, handles byte-aligned 8-bit IntraPCM payloads, fills caller-owned frame-MB neighbors/caches, decodes intra/P/B non-direct syntax, top-level B_Direct residual payloads, and B 8x8/B_SUB_4x4 direct sub-macroblocks, writes raw intra4x4/chroma-pred table state before applying FFmpeg's current-cache availability rewrites for reconstruction, and writes macroblock state. high-bit-depth IntraPCM, MBAFF/field pictures, and row-threaded deblocking remain pending. |
| `libavcodec/h264_cabac.c` inter ref/MVD cache fill inside `ff_h264_decode_mb_cabac` | `internal/h264/cabac_motion.go` `fillCABACInterMotionCache` | translated | unit, FFmpeg frame-MD5 oracle fixture | Applies decoded CABAC refs and signed MVDs to 16x16, 16x8, 8x16, and 8x8 sub-MB MV/MVD cache rectangles, including FFmpeg's clipped unsigned MVD-context magnitude writes, direct-sub ref-cache top-left repair, and direct-sub MVD zeroing. MBAFF and full complex slice-loop call-site wiring remain pending. |
| `libavcodec/h264_cabac.c` final CABAC MB state writes and `h264_mvpred.h` CABAC `write_back_motion`/P-skip/B-skip MVD table behavior | `internal/h264/macroblock_state.go` CABAC write-back helpers | translated | unit, FFmpeg frame-MD5 oracle fixture | Persists CABAC intra/inter/P-skip/B-skip/IntraPCM CBP, qscale, MB type, slice table, raw chroma-pred table state, raw intra4x4 modes, non-zero counts, motion/ref state, direct-sub state, and CABAC MVD tables, including the FFmpeg `0xf7ef` IntraPCM CBP-table sentinel and zeroed direct-skip/direct-sub MVD rows. MBAFF and full complex slice-loop call-site wiring remain pending. |
| `libavcodec/h264_cabac.c` `ff_h264_decode_mb_cabac` frame-MB entropy-to-state order | `internal/h264/cabac_frame.go` `decodeCABACFrameSliceMacroblock`, `decodeCABACFrameMacroblock` | translated | unit, FFmpeg frame-MD5 oracle fixture | Handles CABAC P-skip/B-skip flags and write-back, carries FFmpeg's running `sl->qscale` and `last_qscale_diff`, reads the CABAC MB type for non-skip macroblocks, performs 8-bit IntraPCM byte-stream pointer backup/reinitialization, fills caller-owned frame-MB neighbors/caches, decodes intra/P/B non-direct prediction/ref/MVD/CBP/dQP/residual syntax plus top-level and B 8x8/B_SUB_4x4 sub-macroblock temporal/spatial B_Direct residual syntax, writes raw intra4x4/chroma-pred table state before applying FFmpeg's current-cache availability rewrites for reconstruction, and writes macroblock state. Covered by Main-profile CABAC IDR/P plus High 4:2:0 32x32 CABAC 8x8-DCT, temporal/spatial-direct B, and 64x64 CABAC B 8x8/B_SUB_4x4 direct-sub public fixtures. High-bit-depth IntraPCM, MBAFF/field pictures, and row-threaded deblocking remain pending. |
| `libavcodec/h264idct_template.c` 8-bit IDCT/DC/dequant wrappers | `internal/h264/idct.go` | translated | unit, compiled C oracle | Ports source-shaped 8-bit 4x4/8x8 inverse transform add, DC-only add, add16/add16intra/idct8_add4/add8/add8_422 dispatch, luma DC dequant IDCT, 4:2:0/4:2:2 chroma DC dequant IDCT, and frame block offsets. The transform-bypass reconstruction call sites now bypass these kernels at qscale 0; high-bit-depth kernels, SIMD dispatch, field block offsets, and non-simple reconstruction integration remain pending. |
| `libavcodec/h264addpx_template.c` 8-bit `ff_h264_add_pixels4`, `ff_h264_add_pixels8` | `internal/h264/dsp.go` `h264AddPixels4Clear`, `h264AddPixels8Clear` | translated | unit, compiled C oracle, FFmpeg frame-MD5 oracle fixture | Ports transform-bypass residual add/clear with FFmpeg's wrapping 8-bit pixel addition, now wired through simple frame-MB qscale-0 lossless reconstruction for luma and chroma blocks. High-bit-depth add-pixels remains pending. |
| `libavcodec/h264dsp_template.c` 8-bit `weight_h264_pixels{16,8,4,2}`, `biweight_h264_pixels{16,8,4,2}` | `internal/h264/dsp.go` `h264WeightPixels`, `h264BiweightPixels` | translated | unit, compiled C oracle | Ports source-shaped weighted and biweighted prediction math for 8-bit widths 2/4/8/16, including unsigned offset cast behavior and clipping. High-bit-depth variants, SIMD dispatch, and reconstruction-loop wiring remain pending. |
| `libavcodec/h264dsp_template.c` 8-bit luma/chroma loop filters | `internal/h264/dsp.go` `h264*LoopFilter*` | translated | unit, compiled C oracle | Ports 8-bit vertical/horizontal luma and chroma deblocking kernels, MBAFF horizontal variants, 4:2:2 chroma variants, and intra variants with source-shaped threshold/clip math. Generic simple frame-picture strength/call-site wiring lives in `internal/h264/loop_filter.go`; high-bit-depth variants and SIMD dispatch remain pending. |
| `libavcodec/h264pred_template.c` 8-bit luma/chroma prediction and H/V add wrappers | `internal/h264/pred.go` | translated | unit, compiled C oracle, FFmpeg frame-MD5 oracle fixture | Ports 8-bit vertical/horizontal/DC/left-DC/top-DC/constant/plane prediction for 16x16 luma and 4:2:0/4:2:2 chroma blocks, 4x4 luma DC/angular predictors, 8x8l filtered-edge predictors, and H.264 lossless vertical/horizontal/filter-add wrappers over block offsets. The simple frame-MB reconstruction call sites now consume the intra16x16, intra4x4, intra8x8l, chroma predictors, and lossless pred-add wrappers. Mad-cow unavailable-neighbor chroma modes, high-bit-depth variants, and remaining non-simple prediction call-site wiring remain pending. |
| `libavcodec/h264chroma_template.c` 8-bit `put/avg_h264_chroma_mc{1,2,4,8}` | `internal/h264/chroma.go` | translated | unit, compiled C oracle | Ports source-shaped 8-bit chroma motion-compensation put/avg kernels for widths 1/2/4/8, preserving copy, horizontal-only, vertical-only, and bilinear fractional-offset branches and FFmpeg rounding. High-bit-depth variants, SIMD dispatch, and full reconstruction wiring remain pending. |
| `libavcodec/h264qpel_template.c` 8-bit `put_h264_qpel{2,4,8,16}` and `avg_h264_qpel{4,8,16}` | `internal/h264/qpel.go` | translated | unit, compiled C oracle | Ports source-shaped 8-bit luma quarter-pel motion-compensation kernels over all 16 fractional positions, preserving horizontal, vertical, HV lowpass, l2 rounded-average composition, clipping, and avg/put final stores. High-bit-depth variants, SIMD dispatch, and full reconstruction wiring remain pending. |
| `libavcodec/videodsp_template.c` 8-bit `ff_emulated_edge_mc` | `internal/h264/motion_comp.go` `h264EmulatedEdgeMC` | translated | unit, compiled C oracle, FFmpeg frame-MD5 oracle fixture | Ports the edge-padded byte helper needed by H.264 motion compensation with caller-owned scratch and independent scratch/source strides for edge blocks wider than the reference frame stride. Non-H.264 video DSP surfaces and high-bit-depth variants remain outside the current decoder slice. |
| `libavcodec/h264_mb.c` `mc_dir_part`, `mc_part_std`, `mc_part_weighted`; `libavcodec/h264_mc_template.c` frame-MB `hl_motion` | `internal/h264/motion_comp.go` | translated | unit, compiled C oracle, FFmpeg frame-MD5 oracle fixture | Ports source-shaped 8-bit standard and weighted frame-MB motion-compensation dispatch for 16x16, 16x8, 8x16, and all 8x8 subpartition shapes over 4:2:0/4:2:2 and qpel-shaped 4:4:4 planes, including edge emulation, explicit weights, implicit bipred consumption, and pre-resolved direct motion caches with caller-owned scratch. Qpel and chroma MC now support separate source/destination strides so 16-wide frames can use FFmpeg-sized edge blocks. Explicit weighted P, 4:4:4 motion, explicit no-skip B motion, temporal/spatial-direct B motion, CAVLC/CABAC B 8x8 and B_SUB_4x4 direct-sub motion, and DPB-fed non-neutral implicit B weights are wired through the simple public frame path; field/MBAFF remapping, reference progress waits, and high-bit-depth/SIMD dispatch remain pending. |
| `libavcodec/h264_mb.c` `hl_decode_mb_predict_luma`, `hl_decode_mb_idct_luma`; `libavcodec/h264_mb_template.c` simple 8-bit frame-MB reconstruction subset | `internal/h264/reconstruct.go` | translated | unit, compiled C oracle, FFmpeg frame-MD5 oracle fixture | Ports the simple frame-MB call-site order for 8-bit IntraPCM, intra16x16, intra4x4, intra8x8, and inter frame macroblocks: raw PCM copy-out, chroma prediction, luma prediction, directional top-right fallback, top-left/top-right 8x8l availability masks, luma IDCT add, chroma DC dequant, and chroma IDCT add over 4:2:0/4:2:2, with luma-only reconstruction for monochrome streams. The qscale-0 transform-bypass branch now follows FFmpeg's raw luma-DC mapping, add-pixels luma/chroma residual adds, and High 4:4:4 Predictive vertical/horizontal pred-add wrappers, proved by CAVLC/CABAC qp=0 yuv420p lossless public frame parity. The 4:4:4 path ports `hl_decode_mb_444_simple_8` for simple 8-bit frames by applying luma prediction/IDCT or transform-bypass add per plane and IntraPCM 16x16 copy-out per plane. High-bit-depth IntraPCM, high-bit-depth transform-bypass, border exchange/deblocking, field/MBAFF, and row-threaded/deblocking-enabled slice-loop scheduling remain pending. |
| `libavcodec/h264_cabac.c` remaining CABAC macroblock/residual integration | none | stubbed | not yet proved | Pending MBAFF/field-picture paths and reconstruction-facing transform call sites after the translated CABAC frame-MB handoff. |
| `libavcodec/h264_slice.c` remaining slice decode | none | stubbed | not yet proved | Pending row-threaded deblocking/border exchange, error resilience, slice threading, CABAC bitstream overread policy, field/MBAFF pictures, FMO, high-bit-depth paths, field/MBAFF implicit weights, and complete delayed-output semantics outside the simple frame-MB decode/reconstruct loop. |
| `libavcodec/h264_mb.c` remaining reconstruction/border paths, `h264_direct.c`, `h264_refs.c`, `h264_picture.c` | none | stubbed | not yet proved | Pending row-time border exchange, gap/reference picture ownership, field/MBAFF picture ownership, field/MBAFF implicit weights, and broad public picture output beyond the simple progressive explicit-ref subset. |
| `libavcodec/h264dsp*`, remaining `h264pred*`, high-bit-depth `h264qpel*`/`h264chroma*`/`h264idct*`/`h264addpx*` | none | stubbed | not yet proved | Pending mad-cow unavailable-neighbor chroma prediction modes, high-bit-depth transform-bypass/transform/add/weight/deblock/prediction/qpel/chroma variants, and SIMD/architecture dispatch after byte parity. |
| `libavcodec/h264dec.c` remaining public decoder integration | none | stubbed | not yet proved | Pending full DPB/reference picture ownership beyond the simple progressive explicit/direct P/B path, complete field/gap/error-resilience MMCO behavior, field/MBAFF implicit weights, full libavcodec delayed-output semantics beyond the simple public API, packet side-data types beyond `AV_PKT_DATA_NEW_EXTRADATA`, A53/AFD, and S12M, field pairing, field/MBAFF and late-SEI timing/interlace behavior beyond the simple picture-timing frame flags, error recovery, threading, and public decode output beyond the simple progressive reference subset. |
